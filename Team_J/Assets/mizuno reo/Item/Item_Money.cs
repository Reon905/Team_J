using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Item_Money : MonoBehaviour
{
    public int value = 0;  //整数値を設定できる(ポイントの設定)

    // Start is calleddefore the first frame update
    private void Start()
    {
        
    }
    //Update is called once per frame
    private void Update()
    {
        
    }
}
